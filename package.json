{
  "name": "source-maps",
  "version": "1.0.7",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha ./test/*.test.js",
    "benchmark": "node ./bench/run",
    "prebuild": "prebuildify --napi --strip --tag-libc",
    "rebuild": "rm -rf build && yarn --build-from-source",
    "postinstall": "node-gyp-build"
  },
  "files": [
    "src",
    "prebuilds",
    "index.js",
    "package.json",
    "binding.gyp",
    "README.md"
  ],
  "binary": {
    "napi_versions": [
      3
    ]
  },
  "dependencies": {
    "node-addon-api": "^1.6.2",
    "node-gyp-build": "^4.2.1"
  },
  "devDependencies": {
    "mocha": "^7.1.0",
    "prebuildify": "^3.0.4",
    "tiny-benchy": "^1.0.0"
  }
}
