{
  "name": "source-maps",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha ./test/*.test.js",
    "benchmark": "node ./bench/run",
    "prebuild": "prebuild -t 3 -r napi --strip",
    "rebuild": "node-gyp rebuild"
  },
  "binary": {
    "napi_versions": [
      3
    ]
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^2.0.0",
    "prebuild-install": "^5.3.3"
  },
  "devDependencies": {
    "@parcel/utils": "^2.0.0-nightly.133",
    "clone": "^2.1.2",
    "mocha": "^7.1.0",
    "prebuild": "^10.0.0",
    "source-map": "^0.7.3",
    "tiny-benchy": "^1.0.0"
  }
}
