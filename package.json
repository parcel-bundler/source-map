{
  "name": "source-maps",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha ./test/*.test.js",
    "benchmark": "node ./bench/run",
    "prebuild": "prebuildify --napi --strip --target node@10.0.0",
    "rebuild": "rm -rf build && yarn",
    "install": "node-gyp-build"
  },
  "files": [
    "src",
    "prebuilds",
    "index.js",
    "package.json",
    "binding.gyp",
    "README.md"
  ],
  "binary": {
    "napi_versions": [
      3
    ]
  },
  "dependencies": {
    "node-addon-api": "^2.0.0",
    "node-gyp-build": "^4.2.1"
  },
  "devDependencies": {
    "@parcel/utils": "^2.0.0-nightly.133",
    "mocha": "^7.1.0",
    "prebuildify": "^3.0.4",
    "source-map": "^0.7.3",
    "tiny-benchy": "^1.0.0"
  }
}
