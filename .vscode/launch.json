{
  "version": "0.2.0",
  "configurations": [{
     "type": "lldb",
     "request": "launch",
     "name": "Launch Program",
     "preLaunchTask": "npm: build:dev",
     "program": "node",
     "args": [
        "${workspaceFolder}/debug.js"
     ],
     "cwd": "${workspaceFolder}"
  }]
}
